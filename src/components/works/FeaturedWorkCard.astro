---
import { Image } from "astro:assets";

import {
  formatPublishedDate,
  getPrimaryUrl,
  kindLabels,
  type WorkEntry,
} from "./work-card";

interface Props {
  work: WorkEntry;
  variant: "card" | "list";
  slideIndex: number;
  slideCount: number;
}

const { work, variant, slideIndex, slideCount } = Astro.props;
const { data } = work;
const url = getPrimaryUrl(data.links) ?? "#";
const Heading = "h3";
---

<article
  class:list={["featured-item", `variant-${variant}`]}
  data-featured-slide
  aria-label={`${slideIndex + 1} / ${slideCount}`}
>
  <a class="featured-card" href={url}>
    <figure class="featured-image">
      <Image
        src={data.cover.image}
        alt={data.cover.alt}
        widths={[480, 720, 960]}
        sizes={variant === "card"
          ? "(min-width: 768px) 58vw, 88vw"
          : "(min-width: 768px) 24vw, 88vw"}
        loading="eager"
      />
    </figure>
    <div class="featured-copy">
      <div class="work-meta">
        <span>{kindLabels[data.kind]}</span>
        <time datetime={data.publishedAt.toISOString()}>
          {formatPublishedDate(data.publishedAt)}
        </time>
      </div>
      <Heading>{data.title}</Heading>
      <p>{data.summary}</p>
    </div>
  </a>
</article>

<style>
  .featured-item {
    flex: 0 0 100%;
    min-width: 0;
    scroll-snap-align: start;
  }

  .featured-card {
    display: block;
    height: 100%;
    color: inherit;
    text-decoration: none;
  }

  .featured-image {
    position: relative;
    aspect-ratio: 4 / 3;
    overflow: hidden;
    background: var(--color-dessert-dark-1);
    border: 1px solid var(--color-dessert-dark-2);

    img {
      display: block;
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 0.45s cubic-bezier(0.16, 1, 0.3, 1);
    }
  }

  .featured-copy {
    padding-top: 16px;

    h3 {
      margin-bottom: 8px;
      font-family: var(--font-kiyosuna-sans);
      font-size: clamp(1.45rem, 6vw, 2rem);
      font-weight: 700;
      line-height: 1.25;
      color: var(--color-empress-dark-2);
    }

    p {
      font-size: 0.88rem;
      line-height: 1.7;
      color: var(--color-empress-dark-1);
    }
  }

  .work-meta {
    display: flex;
    gap: 16px;
    align-items: baseline;
    justify-content: space-between;
    margin-bottom: 8px;
    font-family: var(--font-suse-mono);
    font-size: 0.67rem;
    color: var(--color-empress-base);
    text-transform: uppercase;
    letter-spacing: 0.1em;

    span {
      color: var(--color-empress-dark-2);
    }
  }

  @media (hover: hover) {
    .featured-card:hover {
      .featured-image img {
        transform: scale(1.018);
      }

      h3 {
        color: var(--color-text);
        text-decoration: underline;
        text-decoration-thickness: 1px;
        text-underline-offset: 0.16em;
      }
    }
  }

  @media (width >= 768px) {
    .variant-card {
      grid-row: 1 / 3;

      .featured-image {
        aspect-ratio: 16 / 10;
      }

      .featured-copy h3 {
        font-size: clamp(2rem, 3.2vw, 2.75rem);
      }
    }

    .variant-list {
      .featured-card {
        display: grid;
        grid-template-columns: minmax(116px, 42%) minmax(0, 1fr);
        gap: 20px;
        align-items: start;
      }

      .featured-copy {
        padding-top: 0;

        h3 {
          font-size: clamp(1.2rem, 2vw, 1.55rem);
        }
      }
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .featured-image img {
      transition: none;
    }
  }
</style>
